{% extends 'base.html.twig' %}

{% block title %}Consulta{% endblock %}

{% block body %}
    <h1>Consulta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consultum.id }}</td>
            </tr>
            <tr>
                <th>Sintomas</th>
                <td>{{ consultum.sintomas }}</td>
            </tr>
            <tr>
                <th>FotoSintomas</th>
                <td>{{ consultum.fotoSintomas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('consulta_index') }}">back to list</a>

    <a href="{{ path('consulta_edit', {'id': consultum.id}) }}">edit</a>

    {{ include('consulta/_delete_form.html.twig') }}
{% endblock %}
